# Base URL for canonical tags + OAuth callbacks
NEXTAUTH_URL=http://localhost:3000

# Auth secrets (generate for prod)
NEXTAUTH_SECRET=replace-me
JWT_SECRET=replace-me

# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/receipt_maker

# Optional: public site URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000

